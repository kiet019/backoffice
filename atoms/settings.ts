import { atom } from 'recoil'

export const sidebarCompactState = atom({
  key: 'sidebarCompact',
  default: 0
})
export const showMobileSideBarState = atom({
  key: 'showMobileSideBar',
  default: 0
})
